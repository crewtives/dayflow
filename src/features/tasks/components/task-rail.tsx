const groups = [{ name: "En foco", tasks: ["Diseñar el flujo del prototipo"] }, { name: "Pendiente", tasks: ["Responder correos importantes", "Definir preguntas para las pruebas"] }, { name: "Hecho", tasks: ["Ordenar objetivos de la semana"] }];
export function TaskRail() { return <aside aria-labelledby="tasks-title" className="border-t border-paper-mid bg-paper-bright p-6 md:border-l md:border-t-0 md:p-7"><header className="mb-5"><p className="font-label text-xs tracking-[.14em] text-vermilion-deep">ESTADO</p><h2 className="mt-1 text-2xl font-medium tracking-tight" id="tasks-title">Tareas</h2></header><div className="grid gap-6">{groups.map((group) => <section className="border-t border-vermilion-wash" key={group.name}><header className="flex min-h-12 items-center justify-between"><h3 className="font-label text-xs tracking-[.1em]">{group.name.toUpperCase()}</h3><span className="grid size-6 place-items-center rounded-full border border-paper-mid text-xs">{group.tasks.length}</span></header><ul className="grid gap-2">{group.tasks.map((task) => <li className="border border-paper-mid bg-paper p-3 text-sm" key={task}>{task}</li>)}</ul></section>)}</div></aside>; }
